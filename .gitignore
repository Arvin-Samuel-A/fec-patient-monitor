# ── Certificates (regenerated via scripts/gen-certs.sh) ──
certs/

# ── Python ──
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/
uv.lock

# ── Go ──
fog/fog-service
fog/fog

# ── macOS ──
.DS_Store
*.swp

# ── IDE ──
.vscode/
.idea/

# ── Docker (local) ──
*.tar
.env

# ── Lambda packaging artifacts ──
lambda/notifier/build/
lambda/notifier/notifier.zip
lambda/notifier/.pack-venv/

# ── Vite dashboard artifacts ──
dashboard/node_modules/
dashboard/dist/
dashboard/.vite/