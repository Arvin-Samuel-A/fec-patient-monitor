apiVersion: v1
kind: Service
metadata:
  name: fog-service
spec:
  type: NodePort
  selector:
    app: fog-service
  ports:
  - name: fog-http
    port: 8080
    targetPort: 8080
    nodePort: 30445
    protocol: TCP
